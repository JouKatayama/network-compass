# NC-002 — Canonical Domain Models

Implement canonical enums/entities/value objects from `docs/domain/domain-model.md` and `docs/domain/interaction-event-model.md`, plus Pydantic/domain schema boundaries needed for later work. Do not implement relationship formulas. Add tests for invariants such as valid participants/normalized identifiers where appropriate. No graph UI or recommendation logic.
