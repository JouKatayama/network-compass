# NC-010 — VS001 E2E + Hardening

**State:** Completed — VS001 GO approved on 2026-08-09

## Objective

Close the remaining Vertical Slice 001 acceptance gaps after Review Gate C: harden valid empty,
partial-data, retry, keyboard/focus, long-name, missing-avatar, responsive, and approximately
60-person states; complete deterministic Playwright coverage; publish the final VS001 acceptance
matrix and a Go/No-Go recommendation. Do not add later Home, Recommendation, Analog, Network Health,
or Network Ramp product behavior.

## Accepted baseline and boundaries

- NC-001 through NC-009 are accepted. Relationship formulas, synthetic fixtures, projection
  membership/privacy, OpenAPI contracts, and the Gate C graph interaction are not recalibrated here.
- Preserve the current-person server boundary, person-only graph, deterministic local expansion,
  camera/coordinate preservation, semantic relationship presentation, and soft 60/hard 80 limits.
- Prefer state/controller/semantic assertions over WebGL pixel snapshots. Use deterministic visual
  evidence for selected desktop, 390px, and 60-person states and keep the hero E2E factual.
- Production SSO and later-slice actions remain explicitly out of scope.

## Implementation plan

1. Record Review Gate C and move NC-009 to completed.
2. Correct the valid empty projection case (focal person only) and use the frozen neutral wording.
3. Add factual partial-profile and large-network notices plus a visible retryable expansion error.
4. Harden combobox active-descendant semantics, drawer focus entry, Esc close, and focus restoration.
5. Exercise deterministic 60-node layout/build/hop filtering within a generous CI-safe time bound.
6. Extend Docker Playwright for hero counts/focus, safe network retry, and a 60-person partial state.
7. Run all repository, Docker, browser, drift, and build checks; publish the VS001 matrix and stop
   for the human NC-010 Go/No-Go decision.

## Target files

- `apps/web/features/network/` and its `graph/` subsystem for presentation/accessibility hardening.
- `apps/web/tests/` and `tests/e2e/` for state, 60-person, hero, retry, and keyboard coverage.
- `docs/testing/vs001-acceptance-matrix.md` and `docs/review-artifacts/` for final evidence.
- Repository status/start/review-gate indexes for the NC-010 handoff.

## Verification requirements

- Node.js 24/pnpm 10 and Python 3.13: format, lint, strict type checks, Vitest, pytest, OpenAPI and
  generated-TypeScript drift, and Next.js production build.
- Docker Compose/PostgreSQL health, migration, deterministic demo reset, and all Playwright tests.
- Browser inspection at desktop and 390×844 with no warning/error logs; factual P018/P067 flow and
  an actual server-expanded 60-person P001 projection.
- `git diff --check`, Python lock check, and a complete PASS/FAIL mapping for every VS001 AC.

## Stop condition

Publish a CI-green Draft PR and a GO or NO-GO recommendation, then stop for the human NC-010
Go/No-Go decision. Do not begin later product slices without that decision.

## Completion evidence

- A valid focal-only projection now renders the frozen neutral empty message instead of an
  unreachable zero-node case. Missing role evidence produces a factual partial-data notice and
  never labels the relationship weak.
- Network, Person Detail, search, and expansion have safe visible loading/error behavior. Expansion
  failure remains retryable in place, and upstream error detail is not rendered.
- The ARIA combobox keeps focus on the input, uses active-descendant option semantics, supports
  ArrowDown/Enter/Escape, and avoids nested interactive controls. Person Detail is a labelled dialog
  that receives focus; Esc/close restores the originating search/control or the graph canvas.
- Missing avatars retain an accessible initials fallback, long identities wrap, the graph canvas is
  keyboard-focusable, and 60-person views provide a non-alarmist soft-limit hint with 1-hop relief.
- Deterministic 60-node layout/build/hop filtering completes below the 1.5 s CI guard while reusing
  the same coordinate map. Browser inspection also expanded the real P001 server projection from 37
  to 60 and retained usable camera/controls.
- Docker Playwright passes 5/5: shell/control smoke; factual P001→P018→P067 hero with 37→38
  expansion, drawer focus and Esc restoration; safe retry; 60-person partial state; API health.
- Desktop and 390×844 browser checks found no warning/error console entries. The 390px drawer had no
  horizontal overflow. Review artifacts:
  - `docs/review-artifacts/NC-010-60-person-network.png` — SHA-256
    `edcbe4ebfa760216367cefbf3f8ff184043f2d5c6707ca06544e38cdd3fefa50`
  - `docs/review-artifacts/NC-010-desktop-p067-focus.png` — SHA-256
    `6cfaf27bb1b9476eb1bc0d898f95748b398a3c8945a8eac32cf2084cc15a1a03`
  - `docs/review-artifacts/NC-010-responsive-p067.png` — SHA-256
    `6c9bf3b1e367c20cb6beb51a6ec07b9e99587456afc0be0422e0925f713f5248`
- `make check` passes on Node.js 24/Python 3.13: Prettier, ESLint, Ruff, strict TypeScript and
  mypy (72 source files), Vitest (20 tests), pytest (79 tests), OpenAPI and generated-TypeScript
  drift, and the Next.js production build. Python lock, Compose config, and `git diff --check` pass.
- `docs/testing/vs001-acceptance-matrix.md` maps all 48 numbered ACs and all Person Detail/UX
  requirements to passing evidence. The recommendation is **GO for VS001 acceptance**, not a
  production-release approval.

## Go/No-Go review checklist

- Confirm the final acceptance matrix accurately represents the reviewed NC-001 through NC-009
  behavior and contains no waived or untested VS001 acceptance criterion.
- Confirm safe empty/partial/error language, keyboard focus flow, and the 60-person state are
  coherent extensions of the Gate C experience.
- Confirm the remaining boundaries (production SSO, user correction, later product slices,
  observability, and production-scale qualification) are correctly treated as future scope rather
  than VS001 blockers.
- Accept **GO** to close VS001, or record a specific blocker as **NO-GO**. Do not begin future slices
  until the human decision is recorded.

## Human Go/No-Go decision

- **GO approved on 2026-08-09.** The human reviewer accepted the final 48-criterion matrix,
  Person Detail/UX evidence, factual hero flow, safe edge states, keyboard focus behavior, and
  practical 60-person projection as a coherent VS001 vertical slice.
- This closes NC-010 and VS001. The decision is not a production-launch approval and does not
  authorize future Home, Recommendation, Analog, Network Health, Network Ramp, production SSO, or
  user-correction work without a new frozen issue/specification.
- PR #9 passed Frontend, Backend/database, and Docker/Playwright CI and is approved for squash merge.
